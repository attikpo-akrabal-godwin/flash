<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>Dashboard</title>

<link href="/static/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.0/font/bootstrap-icons.css">
</head>
<body>
<br/>
<div class="container">
  <div class="row align-items-start">
    <div class="col">
     <a href="../index.html" class="btn btn-light" role="button">Logout</a>
    </div>
    <div class="col">
      Liste des clients
    </div>
    <div class="col">
      il y'a <strong> <%= clientTab.length  %></strong> <strong> client</strong> pour ce  (cette) <strong> <% if(clientTab.length>0){%> <%= clientTab[0].product.name  %>  <%}else{%> <%= product.name %>  <%} %> </strong>  
    </div>
    <div class="col">
      <a href="" class="btn btn-dark" role="button"> telecharger csv</a>
    </div>
    <table class="table ">
  <thead>
    <tr>
      <th scope="col">Nom & prenoms</th>
      <th scope="col">Tél</th>
      <th scope="col">Quartier</th>
      <th scope="col">sexe</th> 
    </tr>
  </thead>
  <tbody>
    <% clientTab.forEach(client=>{  %> 
    <tr>
      
      <td><%= client.name  %> </td>
      <td><%= client.phoneNumber  %> </td>
      <td><%= client.quartier  %> </td>
      <td><%= client.sexe  %> </td>

    </tr>
    <% }) %> 
   
  </tbody>
</table>
  </div>
</div>

</body>
</html>