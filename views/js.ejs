<script>
    function copy(token){
     navigator.clipboard.writeText('localhost:3000/userContact/'+token);
     console.log( '<%= process.env.DOMAIN%>');
     alert(`le lien copié 😊 `)
    }
    let del = document.querySelectorAll('.cool')
    console.log(del);
    let  confirmit = function(event){
     if(!confirm('cette action vas supprimer le produit et ces client ')){
       event.preventDefault()}  ;
       console.log("cool");
    }
    if (del) {
      for (let i = 0; i < del.length; i++) {
        del[i].addEventListener("click",confirmit,false);
      }
    }
    
    setTimeout(() => {
     let affichage= document.querySelector('#affichage')
     if (affichage) {
       affichage.style.visibility = 'hidden';
     } 
    }, 5000);
   
    
   </script>
   